# План развития Mafia TMA

## 📅 Приоритеты (по порядку)

1. **🔴 Приоритет 0: Критичные баги** — чиним прямо сейчас
2. **🟡 Этап 2: Карта и миссии** — визуальная карта, NPC боссы, запуск миссий (план пользователя)
3. **🟢 Этап 3: Полировка** — анимации, иконки, Telegram Native (план Qwen)

---

## 🔴 Приоритет 0: Критичные баги

### Баг 1: `reward_money` миссий = 0
**Симптом:** В dashboard миссии показывают `reward_money: 0`

**Причина:** `MissionService.refill_missions` создаёт `UserMission` без переноса наград из шаблона `Mission`

**Файл:** `src/services/mission_service.py` — метод `refill_missions`

**Решение:**
```python
# При создании UserMission из шаблона:
user_mission = UserMission(
    user_id=user_id,
    mission_id=template.id,
    reward_money=template.reward_money,  # ← добавить
    reward_influence=template.reward_influence,  # ← добавить
    wanted_increase=template.wanted_increase,  # ← добавить
    ...
)
```

---

### Баг 2: `/territories/` возвращает пустой список
**Симптом:** Dashboard показывает 0 территорий, хотя в БД 5 штук

**Причина:** Endpoint `GET /territories/` возвращает только территории пользователя, а их нет

**Файл:** `src/api/routers/territory.py` — `list_territories`

**Решение:**
- Вернуть список всех территорий + флаг `is_mine` для каждой
- Добавить поле `boss` (NPC владелец) для не-захваченных
- Для старта: 1 территория принадлежит игроку, остальные — NPC

---

### Баг 3: Нет стартовой территории у игрока
**Симптом:** При регистрации игрок не получает квартал

**Решение:**
- В `_create_starter_package` добавить привязку к 1-й территории
- Создать `UserTerritory` при регистрации

---

### Баг 4: Нет связи между экранами
**Симптом:** BottomNav не протестирован, не все экраны доступны

**Решение:**
- Проверить все роуты в `router-provider.tsx`
- Убедиться что BottomNav показывает правильные пути
- Добавить экран "Помощь"

---

## 🟡 Этап 2: Карта и миссии (План пользователя)

> Полная документация: [PLAN_PHASE_2.md](PLAN_PHASE_2.md)

### Концепция
- **Главный экран = Карта города** с цветными кварталами
- 🟢 Мой квартал | 🔴🔵🟡 Кварталы NPC боссов
- **Топ-бар:** 💰 1000 | 🌐 25 | ⚠️ 0
- **BottomNav:** 🗺️ Карта | 🏪 Магазин | 👤 Бойцы | ❓ Помощь
- **Приближение района:** увеличенный вид с кружками миссий
- **Клик на миссию:** детали → выбор бойца → старт → награда

### Механика влияния
| Квартал # | Влияние для захвата |
|-----------|---------------------|
| 1 (стартовый) | 0 (уже есть) |
| 2 | 25 |
| 3 | ~32 |
| 4 | ~40 |
| 5 | ~49 |
| 6 | ~59 |
| 7 | ~70 |
| 8 | 80 |

**Формула:** `threshold(n) = 25 + (n-1) * 55/7`

### Формула влияния
```python
def influence_threshold(territory_index: int) -> int:
    if territory_index <= 1: return 25
    if territory_index >= 8: return 80
    return round(25 + (territory_index - 1) * (80 - 25) / 7)
```

### Порядок реализации Этапа 2
1. NPC Boss модель + миграция → seed 8 боссов
2. Territory grid_x/y + neighbors → seed 8 кварталов
3. API `/map/` → отдаёт карту с боссами
4. Компонент карты (SVG) → цветные кварталы
5. BottomNav → 4 кнопки (Карта, Магазин, Бойцы, Помощь)
6. Топ-бар ресурсов → sticky header
7. Экран района → кружки миссий
8. Запуск миссии → таймер → награда
9. Формула влияния → проверка при захвате
10. Анимации + Telegram тема

**Оценка:** ~15-20 часов

---

## 🟢 Этап 3: Полировка (План Qwen)

### Визуал
- [ ] Telegram Native (`themeParams`, `MainButton`, Haptic)
- [ ] Framer Motion анимации
- [ ] Lucide React иконки вместо эмодзи
- [ ] Safe areas, touch targets 44px+
- [ ] Toast уведомления

### Геймплей
- [ ] Flash миссии с таймером
- [ ] Пассивный доход (фоновые задачи)
- [ ] Offline режим (кэш)
- [ ] Wanted cooldown

### Техника
- [ ] React Query prefetch
- [ ] Error boundaries
- [ ] Sentry/логирование

**Оценка:** ~5-8 часов

---

## 📊 Общее состояние

```
✅ Бэкенд: API работает, миграции, seed данные (Territory, NPCBoss, UserTerritory)
✅ Бот: Webhook установлен
✅ Фронтенд: FSD структура, 10 экранов, сборка OK
✅ Миссии: запуск через character selection, таймер, награды
✅ События: poll с бэкенда (GET /user_missions/{id}/active_event)
✅ BottomNav: 4 таба — Карта, Магазин, Бойцы, Помощь
✅ Формулы: influenceThreshold, passiveIncome, influenceCapBonus (shared/lib/territory.ts)
✅ Help-экран: FAQ по геймплею
✅ SVG-карта города: 8 полигонов, hover, glow, береговая линия острова
✅ Экран района: кружки миссий, выбор бойца, запуск миссии
❌ Пассивный доход: нет бэкенд-задачи (cron/websocket)
❌ Визуал: нет Telegram Native, нет Framer Motion анимаций
```

---

## 🔜 Что делать дальше (TODO по приоритету)

### 🔴 Приоритет 1: ~~SVG-карта города~~ ✅ ГОТОВО

**Выполнено:**
- SVG с 8 цветными полигонами кварталов
- Цвета: зелёный = мой, цвет босса = NPC, серый = свободный
- Hover-эффекты (glow, утолщение границы)
- Береговая линия острова + водные волны
- Подписи: название + доход + бейдж владельца
- Клик → панель с деталями → «Открыть район»

---

### 🟡 Приоритет 2: ~~Экран района~~ ✅ ГОТОВО

**Выполнено:**
- `/territories/:id` — детальный экран квартала
- Мини-карта района с сеткой + кружки миссий
- Клик на кружок → выбор бойца → запуск миссии
- Свой район (стартовый) тоже открывается с миссиями
- Кнопка «Захватить район» для чужих территорий
- Требования для захвата (сила, интеллект, ловкость, влияние)

**Что допилить:**
- Фильтрация миссий по territory_id (нужен бэкенд: поле `territory_id` в UserMission)
- Позиции кружков сейчас хардкод — можно привязать к `position_x`/`position_y`

---

### 🟢 Приоритет 3: Пассивный доход (бэкенд)

**Что нужно:**
1. **Background task** каждые 10 минут:
   - Для каждого пользователя с захваченными территориями
   - Начислить `passive_income_money` и `passive_income_influence`
   - Варианты: Celery Beat, APScheduler, или Redis cron
2. **API:** `GET /territories/income` — показать ожидаемый доход (уже есть эндпоинт)
3. **Отображение на MapPage:** "Доход: +50💰 +2🌐 / 10 мин"

**⚠️ Не трогать бэкенд без явной необходимости — спросить перед внесением!**

**Оценка:** 2-3 часа (бэкенд)

---

### 🔵 Приоритет 4: Полировка визуала

1. **Telegram Native:**
   - `themeParams` для цветов фона/текста
   - `MainButton` для ключевых действий (начать миссию, захватить)
   - Haptic feedback при кликах
2. **Анимации:** Framer Motion (плавные переходы, пульсация маркеров)
3. **Иконки:** Lucide React вместо эмодзи (частично уже в CaptureModal)
4. **Safe areas:** отступы для iPhone notch
5. **Touch targets:** минимум 44×44px

**Оценка:** 3-4 часа

---

### 📋 Итоговый порядок

| # | Задача | Статус | Время |
|---|--------|--------|-------|
| 1 | SVG-карта с цветными полигонами | ✅ Готово | |
| 2 | Экран района с кружками миссий | ✅ Готово | |
| 3 | Пассивный доход (cron) | ⏳ Нужно | 2-3ч |
| 4 | Полировка (Native, анимации) | ⏳ Нужно | 3-4ч |
| 5 | Фильтрация миссий по territory_id | ⏳ Бэкенд | 1ч |
| **Итого осталось** | | | **6-8ч** |

---

## 📝 Заметки

- **Карта-референс:** 6 кварталов на острове. Наши 8 территорий из seed-данных нужно сопоставить с 6 зонами (объединить некоторые или расширить карту)
- **SVG-подход:** каждый квартал = `<path>` с координатами из `grid_x`, `grid_y` + предопределённые полигоны
- **Цвета боссов:** хранить в `npc_bosses.color` (hex), использовать для fill полигона
- **Миссии на карте района:** координаты `position_x`/`position_y` уже есть в `UserMission` — позиционировать абсолютно внутри контейнера района
